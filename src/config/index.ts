import Config from "@nestsoft/config";

// import * as fs from "fs";
// import * as path from "path";
// fs.writeFileSync(path.join(__dirname, "../../consul.config.json"), JSON.stringify(Config["_config"])); // tslint:disable-line

export default Config;

